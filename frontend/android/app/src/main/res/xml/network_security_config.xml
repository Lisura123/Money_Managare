<?xml version="1.0" encoding="utf-8"?>
<!-- Only allow cleartext HTTP to local/development hosts. All other traffic uses HTTPS. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator host machine -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <!-- Localhost / iOS simulator -->
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- Common LAN ranges for real device testing (same WiFi network) -->
        <!-- Update to your machine's IP when testing on a real device -->
        <domain includeSubdomains="false">192.168.1.1</domain>
        <domain includeSubdomains="true">192.168.1</domain>
        <domain includeSubdomains="true">192.168.0</domain>
    </domain-config>
</network-security-config>
